import { InductionChecklist } from './induction-checklist.ts';

describe('InductionChecklist', () => {
  it('should create an instance', () => {
    expect(new InductionChecklist()).toBeTruthy();
  });
});
